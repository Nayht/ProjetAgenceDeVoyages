{% extends 'base.html.twig' %}


{% block main %}
    <h1 class="inpage-title">Etape {{ etape.id }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th class="emphase">Id</th>
                <td>{{ etape.id }}</td>
            </tr>
            <tr>
                <th class="emphase">NumeroEtape</th>
                <td>{{ etape.numeroEtape }}</td>
            </tr>
            <tr>
                <th class="emphase">VilleEtape</th>
                <td>{{ etape.villeEtape }}</td>
            </tr>
            <tr>
                <th class="emphase">NombreJours</th>
                <td>{{ etape.nombreJours }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_etape_index') }}">Retour aux étapes</a>
    <br>
    <a href="{{ path('admin_etape_edit', {'id': etape.id}) }}">Editer</a>

    {{ include('back/etape/_delete_form.html.twig') }}
{% endblock %}
